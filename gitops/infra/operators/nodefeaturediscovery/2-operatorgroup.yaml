apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: nodefeaturediscovery-operatorgroup
  namespace: openshift-nfd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  targetNamespaces:
    - openshift-nfd # Target only this namespace